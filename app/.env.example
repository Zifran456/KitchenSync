# Copy this file to .env to run KitchenSync locally.

# MongoDB — defaults to local if not set
# MONGODB_URI=mongodb://localhost:27017/kitchensync

# Session secret — any random string works locally
SESSION_SECRET=change-this-to-a-random-string
